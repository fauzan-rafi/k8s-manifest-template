apiVersion: v1
kind: Service
metadata:
  name: ubuntu-nodeport-svc
spec:
  type: NodePort
  selector:
    app: custom-web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30009